# Datasources vides au demarrage - a ajouter dans l'UI Grafana (Configuration > Data sources)
# ou via variables Railway: PROMETHEUS_INTERNAL_URL, LOKI_INTERNAL_URL, TEMPO_INTERNAL_URL
apiVersion: 1
prune: false
datasources: []
